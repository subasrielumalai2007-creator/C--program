#include <stdio.h>
#include <string.h>
int main() {
    char u[20], p[20];
    scanf("%s %s",u,p);

    if(strcmp(u,"admin")==0 && strcmp(p,"1234")==0)
        printf("Login Successful");
    else
        printf("Invalid Login");
    return 0;
}
