#include <stdio.h>
int main() {
    int income, expense;
    printf("Enter income and expense: ");
    scanf("%d%d", &income, &expense);

    printf("Savings = %d", income - expense);
    return 0;
}
