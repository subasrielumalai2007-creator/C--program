#include <stdio.h>
int main() {
    char s[50];
    int i = 0;
    printf("Enter string: ");
    scanf("%s", s);

    while (s[i] != '\0')
        i++;

    printf("Characters = %d", i);
    return 0;
}
