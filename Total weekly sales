#include <stdio.h>
int main() {
    int s[7], i, total = 0;
    for(i = 0; i < 7; i++) {
        scanf("%d", &s[i]);
        total += s[i];
    }
    printf("Total Sales = %d", total);
    return 0;
}
