#include <stdio.h>
int main() {
    int units;
    float bill;
    printf("Enter units: ");
    scanf("%d",&units);

    bill = units * 5;
    printf("Electricity bill = %.2f", bill);
    return 0;
}
