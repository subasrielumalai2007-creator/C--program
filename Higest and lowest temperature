#include <stdio.h>
int main() {
    int t[7], i, max, min;
    for(i = 0; i < 7; i++)
        scanf("%d", &t[i]);

    max = min = t[0];
    for(i = 1; i < 7; i++) {
        if(t[i] > max) max = t[i];
        if(t[i] < min) min = t[i];
    }
    printf("Max=%d Min=%d", max, min);
    return 0;
}
