#include <stdio.h>
#include <string.h>

int main() {
    char a[20], b[20];
    printf("Enter two strings:\n");
    scanf("%s%s", a, b);

    if (strcmp(a, b) == 0)
        printf("Strings are equal");
    else
        printf("Strings are not equal");

    return 0;
}
