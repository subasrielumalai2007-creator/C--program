#include <stdio.h>
int main() {
    char s[50];
    int i, j = 0;

    printf("Enter string: ");
    gets(s);

    for (i = 0; s[i]; i++)
        if (s[i] != ' ')
            s[j++] = s[i];

    s[j] = '\0';
    printf("Without spaces: %s", s);
    return 0;
}
