#include <stdio.h>
#include <string.h>
int main() {
    char pass[10];
    printf("Enter password: ");
    scanf("%s", pass);

    if(strlen(pass) >= 6)
        printf("Valid password");
    else
        printf("Invalid password");

    return 0;
}
