#include <stdio.h>
#include <ctype.h>

int main() {
    char s[50];
    int i = 0;

    printf("Enter string: ");
    scanf("%s", s);

    while (s[i]) {
        s[i] = toupper(s[i]);
        i++;
    }

    printf("Uppercase: %s", s);
    return 0;
}
