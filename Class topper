#include <stdio.h>
int main() {
    int m[5], i, max;
    for(i=0;i<5;i++)
        scanf("%d",&m[i]);

    max = m[0];
    for(i=1;i<5;i++)
        if(m[i] > max)
            max = m[i];

    printf("Topper marks = %d", max);
    return 0;
}
