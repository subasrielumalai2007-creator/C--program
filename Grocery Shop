#include <stdio.h>
int main() {
    float a,b,c,total;
    printf("Enter price of 3 items: ");
    scanf("%f%f%f",&a,&b,&c);

    total = a + b + c;
    printf("Total bill = %.2f", total);
    return 0;
}
